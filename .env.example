# Root environment — consumed by docker-compose.yml
# Copy to `.env` and adjust for your machine. Never commit the real `.env`.

POSTGRES_USER=motion
POSTGRES_PASSWORD=change_me_for_local_dev
POSTGRES_DB=motion_erp
POSTGRES_PORT=5432
